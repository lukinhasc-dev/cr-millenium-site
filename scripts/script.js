const throttle = (e, t) => { let s; return function () { let i = arguments; s || (e.apply(this, i), s = !0, setTimeout(() => s = !1, t)) } }, header = document.getElementById("mainHeader"), backToTopBtn = document.getElementById("backToTop"); let lastScroll = 0; const handleScroll = throttle(() => { let e = window.pageYOffset; e > 100 && header ? header.classList.add("scrolled") : header && header.classList.remove("scrolled"), e > 500 ? backToTopBtn.classList.add("visible") : backToTopBtn.classList.remove("visible"), updateActiveNavLink(), lastScroll = e }, 100); window.addEventListener("scroll", handleScroll, { passive: !0 }); const mobileMenuBtn = document.getElementById("mobileMenuBtn"), mainNav = document.getElementById("mainNav"); mobileMenuBtn.addEventListener("click", () => { mainNav.classList.toggle("active"), mobileMenuBtn.classList.toggle("active") }); const navLinks = document.querySelectorAll(".nav-link"); navLinks.forEach(e => { e.addEventListener("click", () => { mainNav.classList.remove("active"), mobileMenuBtn.classList.remove("active") }) }), document.querySelectorAll('a[href^="#"]').forEach(e => { e.addEventListener("click", function (e) { e.preventDefault(); let t = this.getAttribute("href"); if ("#" === t) return; let s = document.querySelector(t); if (s) { let i = document.getElementById("mainHeader"), a = i ? i.offsetHeight : 80, n = s.getBoundingClientRect().top + window.pageYOffset - a; window.scrollTo({ top: n, behavior: "smooth" }); let r = document.getElementById("mainNav"), l = document.getElementById("mobileMenuBtn"); r && r.classList.contains("active") && (r.classList.remove("active"), l.classList.remove("active")) } }) }), backToTopBtn.addEventListener("click", () => { window.scrollTo({ top: 0, behavior: "smooth" }) }); const observerOptions = { threshold: .1, rootMargin: "0px 0px -100px 0px" }, fadeInObserver = new IntersectionObserver(e => { e.forEach(e => { if (e.isIntersecting) { let t = e.target.getAttribute("data-delay"); t ? setTimeout(() => { e.target.classList.add("visible") }, parseInt(t)) : e.target.classList.add("visible") } }) }, observerOptions), fadeInElements = document.querySelectorAll(".fade-in-element"); fadeInElements.forEach(e => { fadeInObserver.observe(e) }); const animateCounter = (e, t, s = 2e3) => { let i = 0, a = t / (s / 16), n = e.textContent.includes("+") ? "+" : e.textContent.includes("%") ? "%" : "", r = e.textContent.includes("h"); if (r) { e.textContent = t + "h"; return } let l = setInterval(() => { if ((i += a) >= t) e.textContent = t + n, clearInterval(l); else { let s = Math.floor(i); e.textContent = s + n } }, 16) }, statObserver = new IntersectionObserver(e => { e.forEach(e => { if (e.isIntersecting && !e.target.classList.contains("counted")) { e.target.classList.add("counted"); let t = e.target.textContent, s = parseInt(t.replace(/\D/g, "")); animateCounter(e.target, s) } }) }, { threshold: .5 }), statNumbers = document.querySelectorAll(".stat-number, .stat-value"); statNumbers.forEach(e => { statObserver.observe(e) }); class ClientsCarousel { constructor() { if (this.carousel = document.getElementById("clientsCarousel"), this.prevBtn = document.getElementById("prevBtn"), this.nextBtn = document.getElementById("nextBtn"), this.dotsContainer = document.getElementById("carouselDots"), !this.carousel) return; this.slides = Array.from(this.carousel.querySelectorAll(".client-slide")), this.currentIndex = 0, this.slidesToShow = this.getSlidesToShow(), this.maxIndex = Math.max(0, this.slides.length - this.slidesToShow), this.isAnimating = !1, this.init() } getSlidesToShow() { return window.innerWidth <= 768 ? 1 : window.innerWidth <= 1024 ? 2 : 4 } init() { 0 !== this.slides.length && (this.createDots(), this.updateCarousel(!1), this.attachEvents(), this.startAutoPlay()) } createDots() { let e = this.maxIndex + 1; for (let t = 0; t < e; t++) { let s = document.createElement("button"); s.classList.add("carousel-dot"), 0 === t && s.classList.add("active"), s.addEventListener("click", () => { this.goToSlide(t), this.resetAutoPlay() }), this.dotsContainer.appendChild(s) } } updateCarousel(e = !0) { if (this.isAnimating && e) return; e && (this.isAnimating = !0); let t = this.slides[0].offsetWidth, s = parseFloat(getComputedStyle(this.carousel).gap) || 0, i = -this.currentIndex * (t + s); this.carousel.style.transform = `translateX(${i}px)`; let a = this.dotsContainer.querySelectorAll(".carousel-dot"); a.forEach((e, t) => { e.classList.toggle("active", t === this.currentIndex) }), this.prevBtn && (this.prevBtn.disabled = 0 === this.currentIndex), this.nextBtn && (this.nextBtn.disabled = this.currentIndex === this.maxIndex), e && setTimeout(() => { this.isAnimating = !1 }, 500) } goToSlide(e) { this.isAnimating || (this.currentIndex = Math.max(0, Math.min(e, this.maxIndex)), this.updateCarousel(!0)) } next() { this.currentIndex < this.maxIndex ? this.currentIndex++ : this.currentIndex = 0, this.updateCarousel(!0) } prev() { this.currentIndex > 0 ? this.currentIndex-- : this.currentIndex = this.maxIndex, this.updateCarousel(!0) } attachEvents() { this.prevBtn && this.prevBtn.addEventListener("click", () => { this.prev(), this.resetAutoPlay() }), this.nextBtn && this.nextBtn.addEventListener("click", () => { this.next(), this.resetAutoPlay() }); let e; window.addEventListener("resize", () => { clearTimeout(e), e = setTimeout(() => { let e = this.slidesToShow; this.slidesToShow = this.getSlidesToShow(), e !== this.slidesToShow && (this.maxIndex = Math.max(0, this.slides.length - this.slidesToShow), this.currentIndex = Math.min(this.currentIndex, this.maxIndex), this.dotsContainer.innerHTML = "", this.createDots(), this.updateCarousel(!1)) }, 250) }); let t = 0, s = 0, i = !1; this.carousel.addEventListener("touchstart", e => { t = e.touches[0].clientX, i = !0 }, { passive: !0 }), this.carousel.addEventListener("touchmove", e => { i && (s = e.touches[0].clientX) }, { passive: !0 }), this.carousel.addEventListener("touchend", e => { if (!i) return; i = !1; let a = t - s; Math.abs(a) > 50 && (a > 0 ? this.next() : this.prev(), this.resetAutoPlay()) }), this.carousel.addEventListener("mouseenter", () => { this.pauseAutoPlay() }), this.carousel.addEventListener("mouseleave", () => { this.resumeAutoPlay() }) } startAutoPlay() { this.autoPlayInterval = setInterval(() => this.next(), 4e3) } pauseAutoPlay() { this.autoPlayInterval && clearInterval(this.autoPlayInterval) } resumeAutoPlay() { this.autoPlayInterval || this.startAutoPlay() } resetAutoPlay() { this.pauseAutoPlay(), this.autoPlayInterval = null, this.startAutoPlay() } } document.getElementById("clientsCarousel") && new ClientsCarousel; const updateActiveNavLink = () => { let e = document.querySelectorAll("section[id]"), t = window.pageYOffset + 150, s = document.querySelectorAll(".nav-link"); e.forEach(e => { let i = e.offsetTop, a = e.offsetHeight, n = e.getAttribute("id"); t >= i && t < i + a && s.forEach(e => { e.classList.remove("active"), e.getAttribute("href") === `#${n}` && e.classList.add("active") }) }) }, createParticles = () => { if (window.innerWidth <= 768) return; let e = document.getElementById("particles"); if (e) for (let t = 0; t < 20; t++) { let s = document.createElement("div"); s.style.position = "absolute", s.style.width = 4 * Math.random() + 2 + "px", s.style.height = s.style.width, s.style.background = "rgba(220, 38, 38, 0.3)", s.style.borderRadius = "50%", s.style.left = 100 * Math.random() + "%", s.style.top = 100 * Math.random() + "%", s.style.animation = `floatParticle ${10 * Math.random() + 10}s linear infinite`, s.style.animationDelay = 5 * Math.random() + "s", e.appendChild(s) } }, style = document.createElement("style"); style.textContent = `
    @keyframes floatParticle {
        0% {
            transform: translateY(0) translateX(0);
            opacity: 0;
        }
        10% {
            opacity: 0.5;
        }
        90% {
            opacity: 0.5;
        }
        100% {
            transform: translateY(-100vh) translateX(${100 * Math.random() - 50 + "px"});
            opacity: 0;
        }
    }
`, document.head.appendChild(style); const serviceCards = document.querySelectorAll(".service-card, .quality-card-enhanced, .mvv-card"); serviceCards.forEach(e => { e.addEventListener("mouseenter", function () { this.style.transition = "all 0.3s cubic-bezier(0.4, 0, 0.2, 1)" }) }); let ticking = !1; const parallaxScroll = () => { if (window.innerWidth <= 768) { ticking = !1; return } let e = window.pageYOffset, t = document.querySelector(".hero-pattern"); t && (t.style.transform = `translateY(${.5 * e}px)`); let s = document.querySelector(".shipping-background"); if (s) { let i = document.querySelector(".shipping-section"), a = i.offsetTop, n = e - a; n > -500 && n < 1e3 && (s.style.transform = `translateY(${.3 * n}px)`) } ticking = !1 }; window.addEventListener("scroll", () => { ticking || (window.requestAnimationFrame(parallaxScroll), ticking = !0) }), window.addEventListener("load", () => { document.body.classList.add("loaded"), createParticles() }); const buttons = document.querySelectorAll(".btn"); buttons.forEach(e => { e.addEventListener("click", function (e) { let t = document.createElement("span"), s = this.getBoundingClientRect(), i = e.clientX - s.left, a = e.clientY - s.top; t.style.position = "absolute", t.style.left = i + "px", t.style.top = a + "px", t.style.width = "0", t.style.height = "0", t.style.borderRadius = "50%", t.style.background = "rgba(255, 255, 255, 0.5)", t.style.transform = "translate(-50%, -50%)", t.style.animation = "ripple 0.6s ease-out", t.style.pointerEvents = "none", this.appendChild(t), setTimeout(() => t.remove(), 600) }) }); const rippleStyle = document.createElement("style"); rippleStyle.textContent = `
    @keyframes ripple {
        to {
            width: 200px;
            height: 200px;
            opacity: 0;
        }
    }
`, document.head.appendChild(rippleStyle); const imageObserver = new IntersectionObserver(e => { e.forEach(e => { if (e.isIntersecting) { let t = e.target; t.dataset.src && (t.src = t.dataset.src, t.classList.add("loaded"), imageObserver.unobserve(t)) } }) }), lazyImages = document.querySelectorAll("img[data-src]"); lazyImages.forEach(e => imageObserver.observe(e)); const faqItems = document.querySelectorAll(".faq-item"); faqItems.forEach(e => { let t = e.querySelector(".faq-question"); t.addEventListener("click", () => { let t = e.classList.contains("active"); faqItems.forEach(e => { e.classList.remove("active") }), t || e.classList.add("active") }) }); const form = document.getElementById("form"); if (form) { let e = form.querySelector('button[type="submit"]'); form.addEventListener("submit", async t => { t.preventDefault(); let s = new FormData(form); s.append("access_key", "a8160154-a286-46e1-848e-7365dad5ab9c"); let i = e.textContent; e.textContent = "Sending...", e.disabled = !0; try { let a = await fetch("https://api.web3forms.com/submit", { method: "POST", body: s }), n = await a.json(); a.ok ? (alert("Success! Your message has been sent."), form.reset()) : alert("Error: " + n.message) } catch (r) { alert("Something went wrong. Please try again.") } finally { e.textContent = i, e.disabled = !1 } }) }